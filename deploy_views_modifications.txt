En las funciones deploy_to_host y deploy_to_group, reemplaza:

```python
# No necesitamos pasar target_host si ya está definido en el inventario
mensaje_prueba_setting = GlobalSetting.objects.filter(key='mensaje_prueba').first()
mensaje_prueba = mensaje_prueba_setting.value if mensaje_prueba_setting else ''
extravars = {'mensaje_prueba': mensaje_prueba}
```

Por:

```python
# No necesitamos pasar target_host si ya está definido en el inventario
mensaje_prueba_setting = GlobalSetting.objects.filter(key='mensaje_prueba').first()
mensaje_prueba = mensaje_prueba_setting.value if mensaje_prueba_setting else ''

# Obtener el dominio desde la configuración
domain_setting = GlobalSetting.objects.filter(key='domain').first()
domain_value = domain_setting.value if domain_setting else 'example.com'

extravars = {
    'mensaje_prueba': mensaje_prueba,
    'domain_value': domain_value
}
```
